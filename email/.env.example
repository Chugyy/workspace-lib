# Gmail OAuth — Google Cloud Console → APIs & Services → Credentials → Download JSON
GMAIL_CREDENTIALS_PATH=assets/credentials.json

# IMAP/SMTP — fournisseur email (ex: Hostinger)
IMAP_HOST=imap.hostinger.com
IMAP_PORT=993
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
IMAP_EMAIL=
IMAP_PASSWORD=
